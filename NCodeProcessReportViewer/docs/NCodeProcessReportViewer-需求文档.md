# NC 处理报告可视化查看器需求文档

> **维护说明**：本文件是需求基线，随开发持续维护。代码、界面、流程或发布变化后，应同步更新本文件对应条款的状态标注（见第 6 节「实施状态总览」）；本文件位于 `docs/` 目录，与发布说明、审查与待办、操作记录、测试指南、用户手册一起纳入版本管理。

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 文档名称 | NC 处理报告可视化查看器需求文档 |
| 文档版本 | V1.1 |
| 编写日期 | 2026-08-03 |
| 最后核对 | 2026-08-05（依据已实现状况，补充实施状态标注） |
| 目标平台 | Windows 7（必须支持）的免安装便携式 Windows 程序，并可兼容后续 Windows 版本 |
| 文档状态 | 持续维护（后续开发同步更新） |

## 2. 项目定位

本程序是独立的 NC 后处理报告查看工具，用于解析 `NCodeProcess` 生成的 `ncodeprocess-report-*.json` 文件。查看器只读报告，不修改 NC 程序、报告或用户配置。

## 3. 文件发现

- 启动后自动扫描查看器 EXE 所在目录及其 `NCodeProcessData` 子目录。
- 自动识别文件名模式 `ncodeprocess-report-*.json`。
- 支持通过“打开报告文件”选择其他位置的 JSON 报告。
- JSON 格式错误或根节点不是对象时，应给出明确错误提示。

> 实施状态：✅ **已实现**。`discover_reports` 扫描 EXE 目录 + `NCodeProcessData`，并兼容旧版 `NCPostProcessData` 目录与 `ncpostprocess-report-*.json` 模式；`open_report` 支持任意位置 JSON；`load_report` 对根节点非对象、`files` 非数组给出明确错误。

## 4. 可视化内容

- 概览页展示输入/输出目录、开始/结束时间以及成功、失败、跳过、移动、删除、警告、错误数量。
- 使用柱状图展示处理结果和校验问题数量。
- 参数统计页展示全部文件或选中文件的 F、S、X、Y、Z 出现次数、最小值、最大值及 G00 数量。
- 校验问题页展示文件、行号、级别、类型、原始文本和建议；错误红色突出，警告橙色突出。
- 修改差异页展示修改说明及 unified diff；新增行绿色背景、删除行红色背景、差异定位信息蓝色显示。
- 原始 JSON 页提供完整数据核对。

> 实施状态：✅ **已实现**。Notebook 五页（概览与可视化 / 参数统计 / 校验问题 / 修改与差异 / 原始 JSON）；概览含 7 项汇总、元信息与双柱状图（Tk Canvas 自绘）；统计支持「全部文件 / 选中文件」切换；问题按 error/warning 红/橙着色；差异页红删绿增、`@@`/头部蓝色；原始 JSON 只读缩进展示。

## 5. 界面要求

- 默认窗口适合 1920×1080 分辨率，约 1290×720 不全屏；默认状态所有表头完整可见，无需拖动，最小窗口尺寸为 1160×640。
- 报告列表与逐文件明细分栏展示。
- 所有长表格和文本区域提供纵向、横向滚动条。
- 选中报告或文件后立即刷新相关页面，不重复读取文件。

> 实施状态：✅ **已实现**。`window_geometry_for_screen` 按屏幕收缩（1366×768 → 1206×640，最小 1160×640）；左栏报告列表 + 文件明细、右栏 Notebook 分页；全部表格/文本区带纵横滚动条；`_on_report_selected`/`_on_file_selected` 选中即刷新。

## 6. 发布要求

- 使用 Python 3.8 和 PyInstaller 5.13.2 打包。
- 提供单文件 `NCodeProcessReportViewer.exe` 和便携 ZIP。

> 实施状态：✅ **已实现**。`build_portable.ps1`（`-CondaEnvironment python38`）产出单文件 EXE、便携目录与 ZIP；发布构建启用随机密钥字节码加密、`-OO` 优化、运行时反调试、调试模块裁剪与 SHA-256 完整性记录（详见 `SECURITY.md`）。

---

## 7. 实施状态总览

> 状态图例：✅ 已实现。对照时间：2026-08-05。

| 章节 | 主题 | 状态 | 说明 |
|---|---|---|---|
| 3 | 文件发现 | ✅ 已实现 | 自动扫描 + NCodeProcessData + 旧版兼容；打开任意 JSON；错误提示 |
| 4 | 可视化内容 | ✅ 已实现 | 概览/统计/问题/差异/原始 JSON 五页 + 柱状图 + 红绿差异高亮 |
| 5 | 界面要求 | ✅ 已实现 | 1290×720 默认 / 1160×640 最小；分栏；纵横滚动；选中即刷新 |
| 6 | 发布要求 | ✅ 已实现 | Python 3.8 + PyInstaller 5.13.2；单文件 EXE + 便携 ZIP |

> **维护约定**：后续开发新功能或调整行为时，同步更新本表与对应章节的状态标注，并更新文档信息表的「最后核对」日期。

## 8. 待确认事项

- ⏳ 暂无待确认项；如新增图表类型、报告聚合或批量导出需求，应在此补充并明确口径。
