# NCodeProcessReportViewer 审查与待办

> 审查对象：`ncodeprocessreportviewer/viewer.py`、`tests/test_report_viewer.py`。已修复项标注状态；未处理项保留为后续待办。

---

## 一、需求符合性审查（对照需求文档）

| # | 需求 | 状态 |
|---|---|---|
| 1 | 自动扫描 EXE 目录 + NCodeProcessData | ✅ **已实现**：`discover_reports` 扫描目录 + 两个数据子目录 |
| 2 | 兼容旧版 NCPostProcessData / ncpostprocess-report | ✅ **已实现**：`DATA_DIR_NAMES` / `REPORT_PATTERNS` 双模式 |
| 3 | 打开报告文件选择其他位置 JSON | ✅ **已实现**：`open_report` + 文件对话框 |
| 4 | JSON 错误/根节点非对象明确提示 | ✅ **已实现**：`load_report` 校验 + `_load_report` 弹窗 |
| 5 | 概览汇总 + 柱状图 | ✅ **已实现**：7 项汇总 + Canvas 双柱状图 |
| 6 | 参数统计（全部/选中文件，F/S/X/Y/Z + G00） | ✅ **已实现**：`iter_stats_rows` + 文件选择联动 |
| 7 | 校验问题红/橙突出 | ✅ **已实现**：error 红、warning 橙加粗 |
| 8 | 修改差异红删绿增、定位蓝 | ✅ **已实现**：unified diff 着色 |
| 9 | 原始 JSON 核对 | ✅ **已实现**：只读缩进展示 |
| 10 | 1290×720 默认、1160×640 最小、表头完整可见 | ✅ **已实现**：`window_geometry_for_screen` + 布局测试锁定 |
| 11 | 分栏、纵横滚动、选中即刷新 | ✅ **已实现** |
| 12 | 单元格悬停浮窗显示完整内容 | ✅ **已实现**（2026-08-06）：5 张表格通用，仅截断内容弹出 |

## 二、代码质量审查

### 1. 逻辑与健壮性

| # | 问题 | 状态 |
|---|---|---|
| 1 | `iter_stats_rows` 中 `"是" if parameter == "G00" ...` 分支恒为假（G00 不在 F/S/X/Y/Z 循环内），属于死分支；G00 状态实际由独立的「发现/未发现」行展示 | ✅ **已清理**（WP-C2）：改为直接 `"否"` |
| 2 | `_draw_bar_chart` 对 `float(value)` 无容错：报告数值字段若为非数值字符串会抛异常 | ⏳ **未处理**：建议 try/except 或预先数值化 |
| 3 | `report_summary` 缺失字段显示 0（`data.get(key, 0) or 0`）：字段缺失与真实 0 无法区分 | 🔶 **轻微**：查看器只读展示可接受；如需严格可改为「无数据」 |
| 4 | 报告列表 iid 用索引字符串（`str(index)`），`refresh_reports` 后与 `report_paths` 保持一致；若报告被外部删除，`_on_report_selected` 索引仍指向旧列表 | ⏳ **未处理**：可改为路径 iid 或刷新时校验存在性 |

### 2. 性能

| # | 问题 | 状态 |
|---|---|---|
| 5 | 报告 JSON 在主线程同步加载与解析，超大报告会阻塞界面 | ⏳ **未处理**：建议后台线程 + 进度提示 |
| 6 | 选中文件刷新时全量重绘统计/问题/差异/原始 JSON，无分页 | 🔶 **轻微**：中等规模报告可接受；超大报告建议分页或增量 |

### 3. 界面/可用性

| # | 问题 | 状态 |
|---|---|---|
| 7 | 柱状图无悬停数值提示与导出 | ⏳ **未处理**（后续建议） |
| 8 | 报告/文件明细校验列与问题页均以红/橙标识，但无「全部错误」筛选项 | ⏳ **未处理**（可选增强） |

### 4. 兼容与安全

| # | 问题 | 状态 |
|---|---|---|
| 9 | Win7 兼容：`ttk` 原生主题 `vista` 在 Win7 可用；`Microsoft YaHei UI` 不可用时回退 | ✅ **已处理**：`main()` 中 try/except TclError + 系统字体回退 |
| 10 | 只读保证：查看器无任何写文件路径，原始 JSON Text 禁用编辑 | ✅ **已确认** |
| 11 | 隐私：无本机路径/邮箱/代理信息入库 | ✅ **已确认** |

## 三、待办清单（按优先级合并）

| 优先级 | 事项 | 来源 |
|---|---|---|
| 中 | 柱状图数值容错（问题 2）；报告列表刷新后索引存在性校验（问题 4） | 逻辑审查 |
| 中 | 大报告后台加载 + 进度反馈（问题 5） | 性能审查 |
| 低 | 清理 `iter_stats_rows` 死分支（问题 1）；汇总缺失值显示「无数据」（问题 3）；分页/增量（问题 6） | 逻辑/性能 |
| 低 | 图表悬停与导出（问题 7）、错误筛选（问题 8） | 可用性 |

### 5. 第三轮收尾配套（2026-08-06）

| # | 事项 | 状态 |
|---|---|---|
| 1 | 运行日志兼容 WP-F2 新数据：选中事件详情预览区（多行 traceback/关键数据）、`log_event_detail` 纯函数、详情列加宽 | ✅ **已处理** |
| 2 | 界面重设计：文件明细移入右侧页签独占全宽；左侧新增「程序列表」（程序+校验，含全部程序/未配对文件行）联动过滤文件明细 | ✅ **已处理** |
| 3 | 窗口加宽至 1500×800（最小 1250×680）；默认页签为「概览与可视化」，文件明细为第二位 | ✅ **已处理** |
| 4 | 柱状图数值容错（问题 2，第三轮收尾 WP-F3）：`chart_number` 对非数值/缺失汇总字段回退 0，异常报告不崩溃 | ✅ **已处理** |
| 5 | `build_portable.ps1` 缩进规整（第三轮收尾 WP-F5）：行为零变化，打包验证通过 | ✅ **已处理** |
| 6 | 查看器增强 WP-14/15：大报告加载状态提示、校验问题级别筛选、导出问题 CSV（UTF-8 BOM） | ✅ **已处理** |
| 7 | APT 计划 WP-A5：新增「APT 信息」页签（`apt_meta_rows`/`apt_summary_rows`，全局摘要 + 单文件规划详情，缺失回退） | ✅ **已处理** |
