# NCodeProcess 审查与待办

> 本文件合并自 `2026-08-04-需求逻辑审查报告.md`（需求符合性审查）与 `2026-08-04-设计审查报告.md`（健壮性/线程/可用性/分发安全审查）。
> 已修复项标注状态与依据；未处理项保留为后续待办。审查对象：`core.py`/`gui.py`/`preferences.py`/`cli.py`/`tests/`。

---

## 一、需求逻辑审查（2026-08-04）—— 逐项状态

### 1. 确认的逻辑问题（Bug）

| # | 问题 | 状态 |
|---|---|---|
| 1 | `add_m03` 的 S 检测未剔除括号注释，会把 M03 加错位置（如 `(FEED S5000 OK)M03`） | ✅ **已修复**：S 循环改用 `line.split("(")[0]` 剔除注释；测试 `test_m03_ignores_spindle_mention_inside_comment` 锁定 |
| 2 | 无分号程序块 M03 插在块尾（`...S2000F1000M03`），需求要求紧贴 S 数值之后 | ✅ **口径已确认（2026-08-05，方案 A）**：`after-s` 维持块尾插入；需求文档 FR-05.4 表述同步为「追加在 S 所在程序块末尾、分号之前」；Batch 2 已提供 `standalone` 策略可选 |

### 2. 需求与实现不一致（需确认取舍）

| # | 问题 | 状态 |
|---|---|---|
| 3 | M03 无法补写时仅警告而非错误 | ✅ **已修复**：自动补写开启且无可插入位置时 `spindle-start` 升级为 error（FR-05.6），测试锁定 |
| 4 | 重复 MSG 字段未按「警告」上报 | ✅ **已修复**：`duplicate-msg-field` warning 进入 issues 与报告 |
| 5 | 第 8 节配置大面积未进 GUI | ✅ **已解决**：Batch 1 + Batch 2 全部 GUI 化（编码/扩展名/允许字符/必填字段/M03 策略/S/F 上下限/结束标记/M06/S/G00/换行/辅助顺序）；**机床行程按用户决定不实施** |
| 6 | 需求文档自身矛盾（特殊刀具存储 / TOOL_ANGLE 小数位 / 未知扩展名） | ✅ **已确认口径**：按 FR-03.8（special_tools.json）、4.3.20（3 位小数）、5.2（未知扩展名忽略）执行 |

### 3. 轻微偏差（保留为待办）

- `apply_header` 删除头部与正文之间已有空行，与 FR-04.6「保留已有格式空行」存在张力 —— **未处理**。
- `extract_tools` 判定「普通立铣刀」仅比较 CUTTER/TOOLNO 直径、未比较圆角（FR-4.3.16）—— **已修复（WP-06，提交 `92fc728`）**：直径与圆角都一致才判普通立铣刀。
- FR-07.2「刀具调用是否包含对应刀具号」「孤立的坐标/进给/转速指令」检查—— **已修复（WP-07，提交 `d44f532`）**：新增 `tool-number-missing`（正文 T 调用无对应头部 Tn，warning）与 `isolated-parameter`（无运动/辅助指令的孤立 F/S 行，warning）。
- FR-01.5「无 MPF 目录应终止处理」：实现仅加 warning 不终止（需求同条允许纯 aptsource 目录继续，属条件性描述）—— **维持现状**。

### 4. 合规确认（抽查通过，概述）

程序名提取与重命名、重复目标按最新覆盖、头部处理（HASS `%` 保留/2500B 默认/非空保护）、图号版次必填、刀具识别与排序、M03 补写（除问题 1/2）、统计、校验（F0/离群/多 S/G00/N 号/结束标记/引号/控制字符/负值）、预览与原子处理、报告保留三份、注册表偏好存储、性能（受控并行/缓存/正则预编译）等均已抽查通过。详见归档 `docs/archive/` 下原始报告（归档目录仅本地保留，git 不跟踪）。

---

## 二、设计审查（2026-08-04）—— 逐项状态

### 1. 线程与生命周期

| # | 问题 | 状态 |
|---|---|---|
| 1 | 后台线程直接调用 Tkinter API（`scan`/`process` 中 `self.after`），窗口销毁后可能抛 TclError 被静默吞掉 | ✅ **已修复**：新增 `App._safe_after`（统一 try/except tk.TclError），`scan`/`process` 线程回调一律经其回主线程（提交 `7840844`） |
| 2 | 扫描无「代际」防护：重复点击会并发多个扫描线程，旧结果可能覆盖新结果 | ✅ **已修复**：`App._scan_generation` 递增序号，`finish_scan(result, generation)` 只接受当前代（提交 `b95ffcc`） |
| 3 | 处理过程无进度反馈（FR-08.3 要求显示进度和当前文件） | ✅ **已修复**：`process_plan` 可选 `progress_callback`；GUI 锁保护进度槽 + 100ms 轮询状态栏「i/N 文件」（提交 `8ffa82d`） |

### 2. 容错与可恢复性

| # | 问题 | 状态 |
|---|---|---|
| 4 | 删除/覆盖不可恢复，无备份/回收站兜底 | ✅ **已修复**：`process_plan` 新增 `backup` 参数，处理前将待处理文件快照到 `backup/YYYYMMDD_HHMMSS/`，报告记录 `backup_dir`；GUI 执行前询问「先备份再清理」（提交 `b2704fc`） |
| 5 | 只读目录无预检（需求 9.3） | ✅ **已修复**：`scan_directory` 探测目录可写性并加入警告，GUI 扫描完成后弹窗提示（提交 `b2704fc`） |
| 6 | 注册表保存静默失败，界面仍提示「已保存」 | ✅ **已修复**：`save_all` 返回后端与位置，状态栏区分「注册表/设置文件」；注册表不可写时回退设置文件 |
| 7 | 异常归类粗糙：MPF 读取异常一律标 `kind="encoding"` | ⏳ **未处理**：建议按 encoding/permission/io 细分 |

### 3. 编码策略

| # | 问题 | 状态 |
|---|---|---|
| 8 | gb18030 兜底几乎不失败 → 错误编码被静默接受 | ✅ **已修复**：`_decode` 按 utf-8 → gb2312 → gbk → gb18030 → cp1252 顺序精确识别并记录编码（`FilePlan.encoding` 写入报告、解析信息页显示）；含 NUL 数据拒绝解码并报 `encoding` 错误（防二进制被静默当文本处理）；设置对话框编码下拉新增 gbk/gb2312（提交 `c551175`）。注：GB18030 可编码全部 Unicode，纯「回环校验」无效，经实验验证后改为上述方案 |

### 4. UI/UX

| # | 问题 | 状态 |
|---|---|---|
| 9 | 「全部程序信息」子窗口固定 1500×620，超出最低分辨率 | ✅ **已修复**：新增 `centered_position` 纯函数与 `App._show_centered`，全部 5 个子窗口（设置/统计/对比/编辑/确认页）居中显示；「全部程序信息」窗口按屏幕适配尺寸（提交 `1f79a6a`） |
| 10 | 未命名程序逐个弹模态框 | ⏳ **未处理**：建议列表式批量确认页 |

### 5. 配置与一致性

| # | 问题 | 状态 |
|---|---|---|
| 11 | GUI 与 CLI 配置面分叉（CLI 支持 `--overwrite`/`--require-m06` 等，GUI 无对应开关） | 🔶 **部分处理**：Batch 2 后 GUI 覆盖大部分配置；CLI 仍有 `--overwrite`/`--keep-intermediate` 等差异项 |
| 12 | 启发式阈值硬编码（F 离群、多 S 警告） | 🔶 **部分处理**：S/F 上下限已可配置（Batch 2）；`feed-outlier`/`multiple-spindle-speeds` 阈值仍硬编码 |

### 6. 文本改写边界

| # | 问题 | 状态 |
|---|---|---|
| 13 | `add_initial_tool_change` 整行替换会改写注释里的刀具号（如 `(T2 备用)` → `(T1 备用)`） | ✅ **已修复**：抽取 `code_part` 统一去注释（add_m03/standalone M03/统计/校验/换刀全部复用）；换刀替换仅作用于代码部分，注释中 T 号保持原样（提交 `92fc728`） |

### 7. 分发与安全

| # | 问题 | 状态 |
|---|---|---|
| 14 | 反调试钩子 + 加密 PYZ + UPX 组合的杀软误报风险 | ⏳ **未处理**：建议发布前实测主流杀软、准备误报申诉说明 |

### 8. 其他细节

| # | 问题 | 状态 |
|---|---|---|
| 15 | MSG 行替换丢失原缩进（FR-4.2.5「尽量保持缩进」） | ⏳ **未处理** |
| 16 | `add_m03`「无法确定位置」提示未进入报告 | 🔶 **部分处理**：失败场景已由 `spindle-start` error 覆盖；`changes` 审计仍不含失败原因 |
| 17 | 无单实例约束，两个 EXE 竞态写 `special_tools.json` 与报告时间戳 | ⏳ **未处理** |

---

## 三、待办清单（按优先级合并）

| 优先级 | 事项 | 来源 |
|---|---|---|
| 高 | 线程安全与生命周期（问题 1-3）：queue 轮询 / 扫描代际防护 / 处理进度反馈 —— **已处理（WP-01，提交 `b95ffcc`/`7840844`/`8ffa82d`）** | 设计审查 |
| 高 | 「全部程序信息」子窗口屏幕适配（问题 9）—— **已处理（WP-02，提交 `1f79a6a`）** | 设计审查 |
| 中 | 备份/回收站兜底、只读目录预检（问题 4-5）—— **已处理（WP-04，提交 `b2704fc`）** | 设计审查 |
| 中 | 编码识别与不确定时提示（问题 8）—— **已处理（WP-05，提交 `c551175`）** | 设计审查 |
| 中 | 注释边界统一（问题 13）与 `extract_tools` 圆角比较—— **已处理（WP-06，提交 `92fc728`）** | 设计+需求审查 |
| 低 | 异常归类细分（问题 7）、批量确认未命名程序（问题 10）、阈值进 Config（问题 12）、MSG 缩进保留（问题 15）、单实例约束（问题 17）、杀软误报实测（问题 14） | 设计审查 |
| 低 | M03 紧贴 S 值的确切口径确认（需求审查问题 2） | 需求审查 |
| 待用户决定 | 机床行程 X/Y/Z 检查 | 需求 7.3（用户已决定暂不实施） |
